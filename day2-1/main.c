#include <stdio.h>
#include <stdlib.h>

int main()
{
   double height,width , erea;
  printf("Enter the h of the rectangle:");
   scanf("%lf",&height);

   printf("Enter the width the rectangle:");
   scanf("%lf",&width);

  if (height <=0  || width <= 0)
  printf("%invalid erea");

      else{ erea= (height*width);
  printf("the erea %lf",erea);}





    }

















