#include <stdio.h>
#include <stdlib.h>

int main()
{

    printf("size of int in memory %d bytes\n" ,sizeof (int));
     printf("\nsize of int in memory %d  bytes\n", sizeof(char));
      printf("size of int in memory %d bytes\n" ,sizeof (float ));
       printf("size of int in memory %d bytes" ,sizeof (double));
    return 0;
}
